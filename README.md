# 🐘 PHP Learning Journey

Welcome to my **PHP Learning Journey** — a collection of examples and small projects created while learning PHP step by step.  
Each file explores a different PHP concept: from basic syntax and loops to file handling, sessions, and date/time logic.

---

## 📁 Project Overview

### 🧠 Core Logic & Control Structures
- 🧒 `age.php` – Age-based condition example  
- 🔁 `do_while_numbers.php` – Using `do…while` loops  
- 🚫 `break_at_five.php` – Breaking a loop at a condition  
- 🎯 `switch-case-example.php` – Switch/Case usage example  

### 💰 Price & Math Calculations
- 🛍️ `bulk_discount_calculator.php` – Simple bulk discount  
- 💸 `final_price.php` – Calculate price including VAT  
- 🧮 `tax_rate.php` – Tax and rate calculation example  
- 🔢 `rounding_numbers.php` – Number rounding demonstration  

### 📅 Date & Time
- ⏰ `day.php` – Detect part of the day (morning/afternoon/night)  
- 🌌 `zodiac_finder.php` – Determine zodiac sign  
- 📆 `dynamic_year_select.php` – Dynamic dropdown with years  

### 🌐 Browser & Device Detection
- 🧭 `browser.php` – Detect user’s browser  
- 📱 `detect_device_browser.php` – Identify device and browser  

### 📦 File & Output Handling
- 📂 `file_lister_with_glob.php` – List files with `glob()`  
- 📝 `formatted_text.php` – Use of `printf`, `sprintf`, `fprintf`  

### 🎨 Arrays & Characters
- 🔡 `char_range_demo.php` – Generate letters with `range()`  

### 🔐 Sessions & Cookies
- 🔑 `session_cookie.php` – Session vs Cookie example  
- 💾 `session_vs_arithmetic.php` – Keep data between page loads  

---

## 🚀 How to Run

1. Clone this repository:  
   ```bash
   git clone git@github.com:phantasmavaga/php-learning-journey.git
