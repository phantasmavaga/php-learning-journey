<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
</head>
<body>
  <h1>🚀 PHP Learning Journey</h1>

  <p>Welcome to my <strong>PHP Learning Journey</strong> — a curated collection of examples and small projects created while learning PHP step-by-step. Whether you're new to PHP or brushing up on fundamentals, this repository is for you.</p>

  <hr>

  <h2>📚 Project Overview</h2>

  <h3>Core Logic & Control Structures</h3>
  <ul>
    <li><code>age.php</code> – Age-based condition example</li>
    <li><code>do_while_numbers.php</code> – Using <code>do…while</code> loops</li>
    <li><code>break_at_five.php</code> – Breaking a loop at a specific condition</li>
    <li><code>switch-case-example.php</code> – Using <code>switch/case</code> structures</li>
  </ul>

  <h3>Price & Math Calculations</h3>
  <ul>
    <li><code>bulk_discount_calculator.php</code> – A simple bulk discount calculation example</li>
    <li><code>final_price.php</code> – Compute final price including VAT</li>
    <li><code>tax_rate.php</code> – Tax and rate calculation example</li>
    <li><code>rounding_numbers.php</code> – Demonstration of number rounding and formatting</li>
  </ul>

  <h3>Date & Time</h3>
  <ul>
    <li><code>day.php</code> – Determine the part of the day (morning/afternoon/night)</li>
    <li><code>zodiac_finder.php</code> – Find zodiac sign based on date</li>
    <li><code>dynamic_year_select.php</code> – Create a dynamic dropdown list of years</li>
  </ul>

  <h3>Browser & Device Detection</h3>
  <ul>
    <li><code>browser.php</code> – Detect the user’s browser type</li>
    <li><code>detect_device_browser.php</code> – Identify device type and browser</li>
  </ul>

  <h3>File & Output Handling</h3>
  <ul>
    <li><code>file_lister_with_glob.php</code> – List files using <code>glob()</code> function</li>
    <li><code>formatted_text.php</code> – Use of <code>printf</code>, <code>sprintf</code>, <code>fprintf</code></li>
  </ul>

  <h3>Arrays & Characters</h3>
  <ul>
    <li><code>char_range_demo.php</code> – Generate letters using <code>range()</code></li>
  </ul>

  <h3>Sessions & Cookies</h3>
  <ul>
    <li><code>session_cookie.php</code> – Session vs Cookie example</li>
    <li><code>session_vs_arithmetic.php</code> – Maintain data between page loads using session or cookie</li>
  </ul>

  <hr>

  <h2>🛠 How to Run</h2>
  <p>Follow the steps below to get this project running on your local machine or development server:</p>
  <ol>
    <li>Clone the repository:<br>
    <code>git clone https://github.com/phantasmavaga/php-learning-journey.git</code></li>
    <li>Navigate into the folder:<br>
    <code>cd php-learning-journey</code></li>
    <li>Ensure you have a PHP environment (e.g., PHP 8.x) installed.</li>
    <li>Start a local server, for example:<br>
    <code>php -S localhost:8000</code></li>
    <li>Open your browser and go to <code>http://localhost:8000</code> (or the specific port you chose).</li>
  </ol>

  <hr>

  <h2>💡 Contribution & Feedback</h2>
  <p>If you find any issues or have suggestions — feel free to open an issue or submit a pull request. Let’s learn together!</p>

  <hr>

  <h2>✅ Commit Example</h2>
  <p>When you add or update files, a good commit message might be:</p>
  <pre><code>git add .<br>git commit -m "🧠 Add example for associative array loop"</code></pre>

  <p>Happy coding! 🎉</p>

</body>
</html>
